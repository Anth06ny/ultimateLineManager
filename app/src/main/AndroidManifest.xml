<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.ultimatelinemanager"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme_Color">
        <activity
            android:name=".StartActivity"
            android:label="@string/app_name"
            android:theme="@style/AppThemeStart">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.SelectTeamActivity"
            android:icon="@drawable/ic_action_group"
            android:label="@string/st_title"
            android:parentActivityName=".StartActivity"
            android:theme="@style/AppTheme_Color">
        </activity>
        <!-- On met l'activité en single top pour garder la team, mais une seul instance possible -->
        <activity
            android:name=".activity.TeamActivity"
            android:label="@string/title_activity_team"
            android:launchMode="singleTop"
            android:parentActivityName=".activity.SelectTeamActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.SelectTeamActivity"/>

        </activity>
        <!-- On met l'activité en single top pour garder la team, mais une seul instance possible -->
        <activity
            android:name=".activity.list_players.TeamPlayerActivity"
            android:label="@string/lpt_title"
            android:launchMode="singleTop"
            android:parentActivityName=".activity.TeamActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ultimatelinemanager.activity.TeamActivity"/>
        </activity>
        <activity
            android:name=".activity.list_players.PickerPlayerActivity"
            android:label="@string/lpt_title">
        </activity>
        <activity
            android:name=".activity.match.TeamMatchActivity"
            android:parentActivityName=".activity.TeamActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ultimatelinemanager.activity.TeamActivity"/>
        </activity>
    </application>

</manifest>
